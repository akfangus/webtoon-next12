/** @type {import('next').NextConfig} */
const nextConfig = {
    reactStrictMode: false,
    images: {
        domains: ['image-comic.pstatic.net', 'kr-a.kakaopagecdn.com'],
    },
};

module.exports = nextConfig;
